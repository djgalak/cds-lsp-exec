export declare class ClsFrame {
    asyncId: number;
    constructor(asyncId: number);
}
export interface ClsFactory<T extends ClsFrame> {
    createFrame(asyncId: number, previousFrame?: T): T;
}
export declare class ContinuationLocalStorage<T extends ClsFrame> {
    private frames;
    private clsFactory;
    private hook?;
    private getFrame;
    constructor(clsFactory: ClsFactory<T>);
    currentFrame(): T;
}

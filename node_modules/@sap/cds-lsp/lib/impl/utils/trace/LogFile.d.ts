export declare class LogFile {
    constructor(logId?: string);
    get numberOfFiles(): number;
    set numberOfFiles(value: number);
    get maxFileSize(): number;
    set maxFileSize(value: number);
    private _folder;
    get folder(): string;
    set folder(newLogFolderpath: string);
    private static ensureFolderExists;
    private fileDescriptor;
    private filename;
    protected currentFileNumber: number;
    private logId;
    static DEFAULT_MAX_FILE_SIZE: number;
    static DEFAULT_NUMBER_OF_FILES: number;
    private _maxFileSize;
    private _numberOfFiles;
    get logfilename(): string;
    protected init(logId?: string): void;
    protected logError(e: any): void;
    private onceRenamed;
    rename(workspaceRoot: string): void;
    protected logFilePath(workspaceRoot: string, rotateNumber: number | string): string;
    append(message: string): void;
    private currentFileCount;
    appendRaw(message: string): void;
    private bumpFileNumber;
    close(): void;
    protected rotateFile(): void;
    protected deleteOrphanedLogFiles(): void;
}

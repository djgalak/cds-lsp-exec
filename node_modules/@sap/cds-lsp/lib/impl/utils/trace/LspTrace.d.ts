import { IConnection } from 'vscode-languageserver';
import { ConfigLevel, ITrace, ITraceConfiguration, ITracer, LspTraceComponent, LspTraceLevel, TraceMessage } from './TracingInterfaces';
export declare const LSP_TRACE_COMPONENT_MAX_LENGTH: number;
export declare class LspTrace implements ITrace {
    private _connection;
    private logFile;
    private traceComponents;
    private get indentLevel();
    increaseIndent(): void;
    decreaseIndent(): void;
    traceLevel(component: LspTraceComponent): LspTraceLevel;
    get logfilename(): string;
    configure(config: ITraceConfiguration): this;
    setConnection(connection: IConnection): void;
    private tracers;
    tracer(component: LspTraceComponent): ITracer;
    conditionalTrace(component: LspTraceComponent, level: LspTraceLevel, message: TraceMessage, x?: any): void;
    doTrace(level: LspTraceLevel, component: LspTraceComponent, message: TraceMessage, x?: any): void;
    private lastTimeTraced;
    renderMessage(level: LspTraceLevel, component: LspTraceComponent, traceMessage?: TraceMessage, x?: any): string;
    private doTraceMessage;
    doTraceToFile(message: string): void;
    addTelemetry(data: any): void;
    static toTraceLevel(configLevel: ConfigLevel): LspTraceLevel;
}

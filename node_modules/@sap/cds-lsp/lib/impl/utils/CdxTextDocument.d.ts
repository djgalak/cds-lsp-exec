import * as LSP from 'vscode-languageserver-protocol';
export declare class CdxTextDocument implements LSP.TextDocument {
    private readonly _uri;
    private readonly _languageId;
    private _version;
    private _content;
    private _splitLines;
    constructor(uri: string, languageId: string, version: number, content: string);
    get uri(): string;
    get languageId(): string;
    get version(): number;
    getText(range?: LSP.Range): string;
    update(event: LSP.TextDocumentContentChangeEvent, version: number): void;
    private getLineOffsets;
    positionAt(offset: number): LSP.Position;
    offsetAt(position: LSP.Position): number;
    get lineCount(): number;
}

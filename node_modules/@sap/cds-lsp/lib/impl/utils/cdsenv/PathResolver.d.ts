/// <reference types="node" />
export declare class PathResolver {
    protected _resolveId(id: string, rootFolder: string): string;
    resolveId(id: string, rootFolder: string): string;
    resolveModule(idChain: string[], startFolders: string[]): string;
    reset(): void;
    private resolveCache;
    getPlatform(): NodeJS.Platform;
    getEnv(key: string): string;
    getGlobalNodeModulesFolders(): string[];
    getLspFolder(): string;
}

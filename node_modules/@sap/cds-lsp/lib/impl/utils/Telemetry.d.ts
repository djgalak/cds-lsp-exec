import { ITrace } from './trace';
export declare class Telemetry {
    private trace;
    constructor(trace: ITrace);
    private telemetryEnabled;
    private intervalToken;
    enable(firstTelegramAfter?: number, repetitionTimeSpan?: number): void;
    disable(): void;
}

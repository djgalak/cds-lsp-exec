import * as LSP from 'vscode-languageserver-protocol';
import { SplitLines } from '../prettyprint/util/StringUtil';
export declare function calculateHashDigest(data: string): string;
export declare function replaceAll(str: string, find: string, replace: string): string;
export interface LspPosition {
    line: number;
    character: number;
}
export declare class TextLines {
    readonly _splitLines: SplitLines;
    constructor(content?: string);
    insert(insertText: string, atPosition: LspPosition): this;
    clone(): TextLines;
    getLines(): string[];
    private restoreLines;
    getText(range?: LSP.Range): string;
}
export declare function markdownTranslation(s: string, isTranslated?: boolean, isTitle?: boolean): string;
export declare function markdownBold(s: string): string;
export declare function markdownItalics(s: string): string;
export declare function parseJson(content: string): any;
export declare function applyTextEdits(text: string, textEdits: LSP.TextEdit[]): string;

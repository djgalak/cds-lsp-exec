/// <reference types="@sap__cds-compiler" />
import { WorkspaceDocuments } from '@sap/cds-compiler';
import { Adapter } from './Adapters';
import { ExtractedCdsEnv } from './cdsenv/ExtractedCdsEnv';
import { LoopDetector } from './FsUtils';
export interface IDependency {
    name: string;
    localPath: string;
}
export declare class NodeModule {
    readonly folder: string;
    private loopDetector;
    private adapter;
    private fetchCdsEnv;
    private tracer;
    private pathResolver;
    constructor(folder: string, loopDetector: LoopDetector, adapter: Adapter, fetchCdsEnv: () => Promise<ExtractedCdsEnv>);
    scanFiles(workspaceDocuments: WorkspaceDocuments): Promise<string[]>;
    fetchDependencies(): Promise<IDependency[]>;
}

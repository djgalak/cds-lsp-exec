import * as LSP from 'vscode-languageserver-protocol';
export interface DiagnosticsState {
    dependentUris: string[];
    diagnostics: LSP.Diagnostic[];
    diagnosticsHash: string;
    uri: string;
    workspaceVersion: number;
}

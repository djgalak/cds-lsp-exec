import * as LSP from 'vscode-languageserver-protocol';
import { DiagnosticsState } from '../utils';
export declare const CLEAN_HASH = "<clean>";
export interface SendDiagnosticsProvider {
    sendDiagnostics(params: LSP.PublishDiagnosticsParams): void;
}
export declare class ValidationState {
    sendDiagnosticsProvider: SendDiagnosticsProvider;
    private tracer;
    constructor(sendDiagnosticsProvider?: SendDiagnosticsProvider);
    compiledFiles: {
        [uri: string]: DiagnosticsState;
    };
    updateFile(uri: string, compiledFile: DiagnosticsState, currentWorkspaceVersion?: number): LSP.PublishDiagnosticsParams | null;
}

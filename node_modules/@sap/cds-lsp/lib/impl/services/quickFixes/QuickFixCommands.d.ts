import * as LSP from 'vscode-languageserver-protocol';
import { ILanguageServer } from '../../ICdsLanguageServer';
export declare enum QuickFixCommands {
    MAINTAIN_TRANSLATION = "maintain-translation"
}
export declare abstract class QuickFixHandler {
    protected lspServer: ILanguageServer;
    constructor(lspServer: ILanguageServer);
    abstract createCommands(params: LSP.CodeActionParams): Promise<LSP.Command[]>;
    abstract execute(params: LSP.ExecuteCommandParams): Promise<any>;
}

import * as LSP from 'vscode-languageserver-protocol';
import { IWorkspaceService } from '../IWorkspaceInterfaces';
import { CompletionContext, ProposalsProvider } from './CompletionStructures';
export declare class TranslationReferenceProposals implements ProposalsProvider {
    private workspaceService;
    readonly name: string;
    constructor(workspaceService: IWorkspaceService);
    fetchProposals(context: CompletionContext): Promise<LSP.CompletionItem[]>;
}

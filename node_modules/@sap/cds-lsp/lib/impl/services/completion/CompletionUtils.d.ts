/// <reference types="@sap__cds-compiler" />
import { XsnLocation, XsnMessage } from '@sap/cds-compiler';
import * as LSP from 'vscode-languageserver-protocol';
import { CompletionContext } from './CompletionStructures';
export declare function replacementRange(aPrefix: string, aSuffix: string, reduce: boolean, position: LSP.Position): LSP.Range;
export declare function filterMessagesInScope({ position }: CompletionContext, message: XsnMessage, relativeProvokerPosition: number): boolean;
export declare function isInRange(range: XsnLocation, cursorPos: LSP.Position): boolean;

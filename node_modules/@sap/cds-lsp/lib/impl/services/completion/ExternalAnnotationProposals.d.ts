/// <reference types="@sap__cds-compiler" />
import { XsnAnnotationAssignment, XsnParseModel } from '@sap/cds-compiler';
import * as LSP from 'vscode-languageserver-protocol';
import { CompletionContext, ProposalsProvider } from './CompletionStructures';
export declare class ExternalAnnotationProposals implements ProposalsProvider {
    private annotationHandlers;
    private readonly tracer;
    readonly name: string;
    constructor();
    fetchProposals(context: CompletionContext): Promise<LSP.CompletionItem[]>;
}
export declare function findAnnotationAtPosition(parseAst: XsnParseModel, cursorPosition: LSP.Position): {
    carrier: any;
    assignment: XsnAnnotationAssignment;
};

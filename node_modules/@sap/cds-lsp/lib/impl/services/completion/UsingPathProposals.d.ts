import * as LSP from 'vscode-languageserver-protocol';
import { CompletionContext, ProposalsProvider } from './CompletionStructures';
interface ProposalClutter {
    start: string;
    end: string;
    labelStart: string;
    labelEnd: string;
}
export declare class UsingPathProposals implements ProposalsProvider {
    readonly name: string;
    static usingFromRegex: RegExp;
    fetchProposals(context: CompletionContext): Promise<LSP.CompletionItem[]>;
    private addMatchingUsingPathProposal;
    protected getSurroundingClutter(firstAllowedPosition: number, rightOfOpeningQuot: boolean, closingQuotPosition: number, cursor: number): ProposalClutter;
}
export {};

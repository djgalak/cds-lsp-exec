import * as LSP from 'vscode-languageserver-protocol';
import { CompletionService } from './CompletionService';
import { CompletionContext, ProposalsProvider } from './CompletionStructures';
export declare class KeywordProposals implements ProposalsProvider {
    private completionService;
    readonly name: string;
    private readonly tracer;
    private snippetProposals;
    constructor(completionService: CompletionService);
    fetchProposals(context: CompletionContext): Promise<LSP.CompletionItem[]>;
    private getKeywordItems;
}

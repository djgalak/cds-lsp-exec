import * as LSP from 'vscode-languageserver-protocol';
import { GenericDefinition } from '../../csn-api';
import { ILspOptions } from '../../utils';
import { ITracer } from '../../utils/trace';
import { DocumentationProvider } from '../DocumentationProvider';
export declare class DocumentationProposals {
    private tracer;
    private _lspOptions;
    private docsForIdentifiers;
    private nextFreeIdentifierDocIndex;
    constructor(tracer: ITracer, _lspOptions: ILspOptions);
    resetIdentifiers(): void;
    attachDocumentation(item: LSP.CompletionItem): void;
    private attachPossibleIdentifierDocumentation;
    attachDocsForDefinition(item: LSP.CompletionItem, documentationProvider: DocumentationProvider, def: GenericDefinition): Promise<void>;
    private attachPossibleModuleDocumentation;
    private docFolder;
    private getDocFolder;
    private _attachDocumentation;
}

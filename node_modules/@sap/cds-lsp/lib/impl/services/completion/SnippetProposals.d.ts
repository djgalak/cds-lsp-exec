import * as LSP from 'vscode-languageserver-protocol';
import { ILspOptions } from '../../utils';
import { ITracer } from '../../utils/trace';
import { IWorkspaceService } from '../IWorkspaceInterfaces';
export declare class SnippetProposals {
    private tracer;
    private workspaceService;
    private _lspOptions;
    constructor(tracer: ITracer, workspaceService: IWorkspaceService, _lspOptions: ILspOptions);
    getSnippetItems(keyword: string, fileUri: string, keywords: string[], isEndOfStatement: boolean): Promise<LSP.CompletionItem[]>;
    private fetchDefaultSuggestedPathForUsingSnippet;
}

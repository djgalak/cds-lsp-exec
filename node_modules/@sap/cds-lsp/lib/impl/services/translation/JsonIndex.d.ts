import * as LSP from 'vscode-languageserver-protocol';
import { I18nIndex, TranslationType } from '../../utils';
import { ExtractedCdsEnv } from '../../utils/cdsenv';
export declare class JsonIndex extends I18nIndex {
    private parsed;
    index(cdsEnv: ExtractedCdsEnv, content: string): void;
    createEntry(cdsEnv: ExtractedCdsEnv, tID: string, translationType: TranslationType): LSP.TextEdit[];
}

/// <reference types="@sap__cds-compiler" />
import { XsnCompileModel } from '@sap/cds-compiler';
import * as LSP from 'vscode-languageserver-protocol';
import { IAnnotationContribution } from '../../api/contributions/IAnnotationContribution';
import { AnnotationAssignmentToken, BlitzIndex } from '../csn-api';
export declare const ANNOTATION_HANDLERS: unique symbol;
export declare class InternalAnnotationHandler implements IAnnotationContribution {
    readonly name: string;
    readonly description: string;
    private external;
    cdsLspContributionKind: 'annotation';
    constructor(name: string, description: string, external: IAnnotationContribution);
    fetchAnnotationDiagnostics(uri: string, ast: XsnCompileModel, relativeLocalPath: string): Promise<LSP.Diagnostic[]>;
    fetchCompletionItems(cursorPosition: LSP.TextDocumentPositionParams, ast: XsnCompileModel, relativeLocalPath: string): Promise<LSP.CompletionItem[]>;
    fetchHover(cursorPosition: LSP.TextDocumentPositionParams, annotation: AnnotationAssignmentToken, index: BlitzIndex): Promise<import("../../api/contributions/IAnnotationContribution").CdsHover>;
}
export declare function registerAnnotationHandlers(handlerPackageNames: string[] | IAnnotationContribution[]): void;

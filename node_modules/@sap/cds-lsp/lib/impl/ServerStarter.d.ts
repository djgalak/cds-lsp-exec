import { IConnection } from 'vscode-languageserver';
import { CdsLanguageServer } from './CdsLanguageServer';
import { EmergencyHooks } from './utils';
import { ITrace, LspTrace } from './utils/trace';
export declare class ServerStarter {
    start(trace?: LspTrace, emergencyHooks?: EmergencyHooks): void;
    protected getAnnotationHandlerNpmPackageNames(): string[];
    protected getPackageJson(): any;
    protected createServer(trace: ITrace): CdsLanguageServer;
    protected createConnection(): IConnection;
}

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const TreeUtil_1 = require("../util/TreeUtil");
const FormattingBasics_1 = require("./FormattingBasics");
class TableExpressionFormatter extends FormattingBasics_1.NodeFormatter {
    constructor(node, context) {
        super(node, context);
        this.onlyTokenChildren = true;
    }
    beforeChildNoCache(node, child, children) {
        function isJoinKeyword(token) {
            if (!token.isKeyword()) {
                return false;
            }
            return [
                'join',
                'inner',
                'outer',
                'left',
                'right',
                'full',
                'cross'
            ].includes(FormattingBasics_1.lowerLexem(token));
        }
        if (isJoinKeyword(child) && !isJoinKeyword(TreeUtil_1.getPreviousToken(child, TreeUtil_1.nonCommentNonAnnotation))) {
            return ['\n'];
        }
        return [];
    }
}
exports.TableExpressionFormatter = TableExpressionFormatter;
//# sourceMappingURL=TableExpressionFormatter.js.map
import { Alignment } from './Alignment';
import { GlobalContext } from './GlobalContext';
export declare class FacsimileAlignment extends Alignment {
    private readonly firstColumn;
    private relativeColumns;
    private firstHardenedLineNumber;
    private firstHardenedColumn;
    constructor(context: GlobalContext, firstColumn: number);
    toString(): string;
    registerFirstHardenedLine(lineNumber: number): void;
    registerFirstHardenedColumn(lineNumber: number, column: number): void;
    setIndentation(lineOffset: number, column: number): void;
    getColumn(hardenedLineNumber: number): number;
}

import { Logger } from '../util/Logger';
import { FormatOptionSet } from './FormatOptionSet';
import { FormatOptionValue, MetaOptionMap, OptionName } from './MetaModel';
export declare const GLOBAL = -1;
export declare class OptionRanges<MM extends MetaOptionMap<MM>> {
    private globalOptions;
    private logger?;
    private areasByOption;
    constructor(globalOptions: FormatOptionSet<MM>, logger?: Logger);
    setLocallyFromString(option: OptionName<MM>, valueString: string, startOffset: number): void;
    setLocally(option: OptionName<MM>, value: FormatOptionValue, startOffset: number): void;
    resetLocally(option: OptionName<MM>, startOffset: number): void;
    setGlobally(option: OptionName<MM>, value: FormatOptionValue): boolean;
    getOption(option: OptionName<MM>, offset: number): FormatOptionValue;
}

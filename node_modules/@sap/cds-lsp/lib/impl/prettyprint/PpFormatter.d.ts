import { CdsFormatOptionSet } from './formatOptions/CdsFormatOptions';
import { ContextRange } from './GlobalContext';
import { PpRuleNode } from './PpRuleNode';
import { PpRuleToken, PpToken } from './PpRuleToken';
import { Position, Range } from './util/TreeUtil';
export interface FormattedSource {
    formatted: string;
    exclusionRanges: ContextRange[];
}
export declare class PpFormatter {
    format(topNode: PpRuleNode, effectiveFormattingOptions: CdsFormatOptionSet, originalSource: string): FormattedSource;
}
export declare function determineRangeTokens(tokens: PpRuleToken[], range?: Range, closingCharacterPosition?: Position): ContextRange;
export declare function rangeToContextRange(tokens: PpToken[], range: Range): ContextRange;
export declare function positionToContextRange(tokens: PpRuleToken[], position: Position): ContextRange;

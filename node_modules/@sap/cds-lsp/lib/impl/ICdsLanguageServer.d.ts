import * as LSP from 'vscode-languageserver-protocol';
import { IWorkspaceService } from './services/IWorkspaceInterfaces';
import { LspTraceLevel } from './utils/trace';
export interface ILanguageServer {
    sendDiagnostics(params: LSP.PublishDiagnosticsParams): void;
    userPopup(level: LspTraceLevel, message: string, ...buttons: string[]): PromiseLike<string>;
    applyWorkspaceEdits(params: LSP.ApplyWorkspaceEditParams): Promise<any>;
    workspaceService: IWorkspaceService;
}

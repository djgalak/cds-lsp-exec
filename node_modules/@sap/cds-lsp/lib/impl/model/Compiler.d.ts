/// <reference types="@sap__cds-compiler" />
import { CdsCompiler, CdsvSourceDictionary, XsnCompileModel, XsnParseModel } from '@sap/cds-compiler';
import { ILspOptions } from '../utils';
import { ModuleLoader } from '../utils/cdsenv';
export interface ICompiler {
    compile(localPaths: string, files: CdsvSourceDictionary, workspaceRoot: string, options?: any): Promise<XsnCompileModel>;
    parse(content: string, localPath: string, workspaceRoot: string, options?: any): Promise<XsnParseModel>;
}
export declare class Compiler implements ICompiler {
    private options;
    protected moduleLoader: ModuleLoader;
    private tracer;
    constructor(options: ILspOptions, moduleLoader?: ModuleLoader);
    private _projectRootToLocalSapCdsPath;
    private hasLocalSapCds;
    compile(localPath: string, files: CdsvSourceDictionary, workspaceRoot: string, options?: any): Promise<XsnCompileModel>;
    parse(content: string, localPath: string, workspaceRoot: string, options?: any): Promise<XsnParseModel>;
    private lastCompiler;
    _getCompiler(workspaceRoot: string): CdsCompiler;
}

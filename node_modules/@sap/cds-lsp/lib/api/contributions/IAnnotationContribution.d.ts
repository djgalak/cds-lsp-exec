/// <reference types="@sap__cds-compiler" />
import { XsnCompileModel } from '@sap/cds-compiler';
import * as LSP from 'vscode-languageserver-protocol';
import { AnnotationAssignmentToken, BlitzIndex } from '../../impl/csn-api';
import { IContribution } from './IContribution';
export interface IAnnotationContribution extends IContribution {
    cdsLspContributionKind: 'annotation';
    fetchCompletionItems?: (cursorPosition: LSP.TextDocumentPositionParams, ast: XsnCompileModel, relativeLocalPath: string) => Promise<LSP.CompletionItem[]>;
    fetchAnnotationDiagnostics?: (uri: string, ast: XsnCompileModel, relativeLocalPath: string) => Promise<LSP.Diagnostic[]>;
    fetchHover?: (cursorPosition: LSP.TextDocumentPositionParams, annotation: AnnotationAssignmentToken, index: BlitzIndex) => Promise<CdsHover | undefined>;
}
export interface CdsHover extends LSP.Hover {
    contents: CdsMarkupContent;
}
export interface CdsMarkupContent extends LSP.MarkupContent {
    kind: 'markdown';
}
